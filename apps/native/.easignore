# Dependencies & build artifacts
node_modules/
.expo/
*.log
android/
ios/
dist/
out/
build/

# Previous build outputs. 'build/' above matches the directory, not the
# build-<timestamp>.apk / .aab files EAS writes into the project root, and
# those get tarred into the archive a --local build unpacks.
*.apk
*.aab
.turbo/

