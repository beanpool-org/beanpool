# Package reports

One file per package, named for its worktree: `<worktree>.md`.

**Write only your own file.** A shared status table would mean every PR after the first hits a merge
conflict on the same lines; per-agent files cannot collide. The template is in
[`../CONTRACTS.md`](../CONTRACTS.md#report-template).
